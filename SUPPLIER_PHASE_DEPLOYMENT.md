# تشغيل مرحلة الإدارة والموردين

هذه المرحلة لا تغيّر قاعدة البيانات الحية تلقائياً. راجع الصفحات محلياً أولاً، ثم نفّذ الخطوات بالترتيب.

## 1. تحديث قاعدة البيانات

افتح Supabase ثم SQL Editor، وشغّل الملفات التالية واحداً بعد الآخر:

1. `supabase/migrations/006_admin_supplier_operations.sql`
2. `supabase/migrations/007_secure_delivery_confirmation.sql`
3. `supabase/migrations/008_customer_request_pricing.sql`

يجب أن تظهر رسالة `Success. No rows returned` بعد كل ملف.

## 2. تحديث خدمة الطلبات

افتح Edge Functions ثم `talagty-api` ثم Code. استبدل محتوى `index.ts` بمحتوى:

`supabase/functions/talagty-api/index.ts`

انشر التحديث واترك خيار **Verify JWT with legacy secret** مغلقاً، لأن الخدمة تستخدم تحققاً خاصاً للمدير والمورد.

## 3. البريد النهائي

الإشعار النهائي يُحفظ دائماً في جدول `email_outbox`. ولإرساله فعلياً، أضف أسرار Edge Functions التالية:

- `ADMIN_REPORT_EMAIL`: البريد المستخدم لتسجيل دخول المدير.
- `RESEND_API_KEY`: مفتاح خدمة Resend.
- `REPORT_FROM_EMAIL`: بريد الإرسال الموثق، مثل `Tallagty <orders@your-domain.com>`.

إذا لم تُضف Resend الآن، لن يضيع التقرير؛ سيظل محفوظاً في `email_outbox` بحالة `pending`.

## 4. نشر الواجهات

ادفع الملفات إلى مستودع GitHub الحالي. ستظهر الصفحات التالية بعد تحديث GitHub Pages:

- `admin.html`: لوحة الإدارة.
- `supplier.html`: تطبيق المورد القابل للتثبيت.
- `track.html`: متابعة العميل وإظهار رمز الاستلام.

## 5. اختبار كامل

1. أنشئ طلباً جديداً من المتجر.
2. سجّل دخول المدير وافتح **الطلبات الجديدة**.
3. راجع الاسم والهاتف والعنوان والمنتجات ثم اضغط **تأكيد الطلب**.
4. افتح **الموردون** واضغط **أضف مندوب**. احتفظ برمز التفعيل الظاهر مرة واحدة.
5. افتح `supplier.html` على هاتف المورد، ثم **تفعيل أول مرة** وأدخل الهاتف وكلمة المرور والرمز.
6. من الإدارة، أسند الطلب إلى المورد.
7. من تطبيق المورد: **بدء تجهيز الطلب** ثم **خرج للتوصيل**.
8. من `track.html` على هاتف العميل، أدخل الهاتف واضغط **استلام الطلبية**.
9. من تطبيق المورد اضغط **تسليم أوردر** وامسح QR، أو أدخل الرمز الاحتياطي.
10. تأكد أن الطلب أصبح **مكتمل**، وأن المخزون نقص، وأن التقرير ظهر في `email_outbox` أو وصل إلى البريد إذا تم إعداد Resend.

## ملاحظات مهمة

- المورد لا يستطيع فتح حسابه من هاتف ثانٍ. يمكن للمدير استخدام **إعادة ربط الهاتف** عند تغيير الجهاز.
- عند انقطاع الإنترنت وقت التسليم، يحتفظ التطبيق بالتأكيد على الهاتف ويرسله تلقائياً بعد عودة الاتصال.
- روابط صور Google Drive يجب أن تكون متاحة لأي شخص لديه الرابط. النظام يحوّل روابط المشاركة الشائعة إلى روابط عرض مباشر.
- تطبيق المورد يعمل كتطبيق PWA على Android وiPhone. على iPhone يتم تثبيته من Safari عبر **مشاركة → إضافة إلى الشاشة الرئيسية**.
