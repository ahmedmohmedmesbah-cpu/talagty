/*
 * Production: replace this address with the HTTPS address of the deployed
 * Tallagty Python order service, without a trailing slash.
 * GitHub Pages can only host the storefront; it cannot execute the API itself.
 */
window.TALLAGTY_API_BASE_URL = 'http://127.0.0.1:8000';
